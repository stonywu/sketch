function setup() {
  createCanvas(400, 400);
  background(255);  // set
}



function mousePressed() {
  
  stroke(0, 0, 255);  // blue
  line(0, 0, width, height);//draw the line
}

function mouseReleased() {
  
  stroke(255, 0, 0);  // red
  line(width, 0, 0, height);//line
}
